pipeline {
    agent { 
        label 'master'
        }
        stages {
        stage("docker run") {
      docker run python
      }
     
     stage("build") {
            steps {
                sh 'python --version'
            }
        }
    }
}
