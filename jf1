pipeline {
    agent { 
        label 'master'
        }
         stages {
        stage("docker run") {
      docker run python:3.5.1
      }
    stages {
        stage('build') {
            steps {
                sh 'python --version'
            }
        }
    }
}
