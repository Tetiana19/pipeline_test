pipeline {
    agent { 
        label 'master'
        }
        stages {
        stage('docker run') {
      sudo docker run python
      }
     
     stage('build') {
            steps {
                sh 'python --version'
            }
        }
    }
}
