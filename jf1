pipeline {
    agent { 
        label 'master'
        }
         stages {
        stage('docker run') {
      docker run python
      }
     }
    stages {
        stage('build') {
            steps {
                sh 'python --version'
            }
        }
    }
}
