pipeline {
    agent { 
        label 'master'
        }
                stage("docker run") {
      docker run python
      }
     
     stage("build") {
            steps {
                sh 'python --version'
            
        }
    }
}
